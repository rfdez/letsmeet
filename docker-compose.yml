version: '3.9'

x-app-service: &default-app
  build: .
  image: rfdez/letsmeet:dev
  command: ''
  volumes:
    - .:/code:delegated

services:
  app:
    <<: *default-app

  meeting-backend:
    <<: *default-app
    command: bash -c "npm run start:meeting:backend"
    ports:
      - '3000:3000'
