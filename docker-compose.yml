version: '3.9'

x-app-service: &default-app
  build: .
  image: rfdez/letsmeet:dev
  command: ''
  volumes:
    - .:/code:delegated

services:
  app:
    <<: *default-app

  user-backend:
    <<: *default-app
    command: bash -c "npm run start:user:backend"
    ports:
      - '3000:3000'
